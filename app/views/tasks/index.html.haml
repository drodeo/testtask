.page-header
  %h1 Все задачи

%table.table
  %thead
    %tr
      %th #
      %th Название
      %th Описание
      %th Статус
      %th Создана
      %th Назначена
  %tbody
    - @tasks.each do |task|
      %tr
        %td= task.id
        %td= task.name
        %td= task.description
        %td= task.state
        %td= task.created_at.to_s(:short)
        %td= task.user.email
